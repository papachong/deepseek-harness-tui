/**
 * @ruhooai/dsh-tui — the terminal UI bundle. This module exports only the
 * package surface; the runtime lives in {@link ./runner.ts} and the
 * answerers in {@link ./answerers.ts}. The bin ({@link ./bin.ts}) drives the
 * runner directly.
 *
 * @module @ruhooai/dsh-tui
 */

export {}

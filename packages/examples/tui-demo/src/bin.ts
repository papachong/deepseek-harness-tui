#!/usr/bin/env node
/**
 * TUI demo bin. External configurations own their bare plugin packages.
 *
 * @module @deepseek-ai/dsh-tui-demo/bin
 */

import { runTuiDemo } from './runner.ts'

await runTuiDemo()

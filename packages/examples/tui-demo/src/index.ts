/**
 * Bin-only app package: boots an external cordis.yml, owns process exit.
 * This module exports no composition plugin; the config chooses whether to
 * load the agent-spine and llm-replay plugins.
 *
 * @module @deepseek-ai/dsh-tui-demo
 */

export {}
